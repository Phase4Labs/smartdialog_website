
  # Migrate website to Vercel

  This is a code bundle for Migrate website to Vercel. The original project is available at https://www.figma.com/design/G40nep9xVRN3fLdsyLSPwv/Migrate-website-to-Vercel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
 
 "# smartdialog_website"